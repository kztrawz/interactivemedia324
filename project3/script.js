$('video').click(function () {
	findImage()
});

function findImage() {
	var x = Math.floor(Math.random() * ) + 1;

	switch (x) {
		case 1:
		changeImage();
		break;

		case 2:
		changeImage();
		break;
	}
}